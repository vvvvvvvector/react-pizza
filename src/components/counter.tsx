import { useState } from 'react';

export const Counter = ({ amount }: { amount: number }) => {
  if (false) {
    return <></>;
  }

  const a = useState();

  return (
    <div>
      <b>{amount}</b>
    </div>
  );
};
